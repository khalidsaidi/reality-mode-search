# Notes

- Environment constraint: Node.js/npm are not installed and outbound network/DNS is unavailable here, so scaffolding and installs cannot be executed. The repo will include the expected files/config so it can be installed and run in a normal dev environment.

