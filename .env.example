# Common Crawl Index Server
# This project uses the Common Crawl URL index (open data, no API key).
# HTTPS is not always available for the index server, so HTTP is the default.
COMMONCRAWL_INDEX_BASE_URL=http://index.commoncrawl.org
# Optional: pin a specific Common Crawl index collection id (otherwise latest is used)
COMMONCRAWL_INDEX_ID=

# Cache / sustainability defaults
CACHE_TTL_SECONDS=604800
SWR_SECONDS=0
STALE_IF_ERROR_SECONDS=604800
DAILY_MISS_BUDGET=1500

# Provisioning helpers (do NOT commit real values)
GITHUB_TOKEN=
VERCEL_TOKEN=
